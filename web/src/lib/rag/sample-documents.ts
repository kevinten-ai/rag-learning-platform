export interface SampleDocument {
  id: string;
  title: string;
  content: string;
}

export const SAMPLE_DOCUMENTS: SampleDocument[] = [
  {
    id: 'sample-rag-overview',
    title: '什么是 RAG？检索增强生成技术详解',
    content: `## 什么是检索增强生成（RAG）

检索增强生成（Retrieval-Augmented Generation，简称 RAG）是一种将信息检索与大语言模型（LLM）相结合的技术架构。它的核心思想是：在大语言模型生成回答之前，先从外部知识库中检索与用户问题相关的文档片段，然后将这些片段作为上下文注入到提示词中，让模型基于真实数据来生成回答。

## 为什么需要 RAG

大语言模型虽然强大，但存在一个根本性的局限：它的知识来源于训练数据，存在时效性截止的问题。模型无法获知训练截止日期之后发生的事件，也无法访问企业内部的私有数据。更关键的是，当模型对某个问题没有足够的训练数据支撑时，它会倾向于"编造"看似合理但实际错误的答案，这就是所谓的"幻觉"问题。RAG 的出现正是为了解决这些痛点——通过引入外部知识源，让模型的回答有据可依。

## RAG 的工作流程

RAG 系统的工作流程分为三个阶段。第一步是检索（Retrieve）：当用户提出问题时，系统将问题转化为向量表示，然后在预先构建的向量数据库中进行语义相似度搜索，找到最相关的文档片段。第二步是增强（Augment）：将检索到的文档片段与用户原始问题组合，构建一个包含丰富上下文的提示词。第三步是生成（Generate）：将增强后的提示词输入大语言模型，模型根据提供的上下文生成准确、有据可查的回答。

## RAG 的核心优势

RAG 技术具有多方面的优势。首先，它显著降低了幻觉的发生率，因为模型的回答基于检索到的真实文档。其次，它实现了知识的动态更新，只需更新知识库即可让系统获取最新信息，无需重新训练模型。此外，RAG 的回答可以追溯来源，增强了系统的可解释性和可信度，用户可以验证回答的依据，这在企业级应用中尤为重要。`,
  },
  {
    id: 'sample-vector-search',
    title: '向量数据库与语义搜索原理',
    content: `## 从文本到向量：嵌入表示

在自然语言处理领域，文本嵌入（Text Embedding）是将文字转换为高维向量的技术。通过预训练的嵌入模型（如 OpenAI 的 text-embedding-ada-002 或火山引擎 Ark embedding），一段文本会被映射为一个固定维度的浮点数数组，例如 1024 维的向量。这些向量能够捕捉文本的语义信息——含义相近的文本在向量空间中的距离更近，而含义不同的文本则相距较远。

## 向量相似度与余弦距离

衡量两个向量之间语义相似度最常用的方法是余弦相似度（Cosine Similarity）。它计算两个向量夹角的余弦值，结果范围在 -1 到 1 之间，值越接近 1 表示两个向量方向越一致，即语义越相似。相比欧氏距离，余弦相似度对向量的长度不敏感，只关注方向，因此在高维文本向量的比较中表现更为稳定和可靠。

## 近似最近邻搜索与 HNSW 索引

在包含数百万条向量的数据库中进行精确的最近邻搜索是非常耗时的——它需要逐一计算查询向量与所有存储向量的距离。为了解决这个效率问题，向量数据库采用了近似最近邻（ANN）算法。其中最流行的是 HNSW（Hierarchical Navigable Small World）算法，它构建了一个多层图结构，类似于跳表的概念：高层图是稀疏的，用于快速定位大致区域；底层图是稠密的，用于精确查找。这种分层结构使得搜索时间从线性降低到对数级别，同时保持了极高的召回率。

## 语义搜索 vs 关键词搜索

传统的关键词搜索依赖词汇的精确匹配，存在明显的局限性。例如，搜索"如何解决模型胡说八道的问题"时，关键词搜索无法匹配到包含"幻觉"这一专业术语的文档。而基于向量的语义搜索能够理解查询的深层含义，将"胡说八道"与"幻觉"关联起来，从而检索到真正相关的内容。这正是语义搜索在 RAG 系统中不可替代的原因——它弥合了用户自然表达与专业知识库之间的语义鸿沟。`,
  },
  {
    id: 'sample-llm-hallucination',
    title: '大语言模型的幻觉问题与解决方案',
    content: `## 什么是大语言模型的幻觉

大语言模型的"幻觉"（Hallucination）是指模型生成的内容看似流畅合理，但实际上包含了虚假、不准确或无中生有的信息。例如，当你询问模型某位学者的论文列表时，它可能会编造出根本不存在的论文标题和期刊名称，而且语气十分确定。这种现象在事实性问答、数据引用和专业领域对话中尤为危险，因为用户很难仅凭文本本身判断其真伪。

## 幻觉产生的根本原因

幻觉的产生有多重原因。首先，大语言模型本质上是一个概率预测系统——它根据前文预测下一个最可能出现的词元（Token），而非从结构化的知识库中查询事实。这意味着它会优先选择统计上最合理的续写，而不是事实上最准确的答案。其次，训练数据存在时效性截止，模型对训练日期之后的事件完全无知，但仍然会尝试回答相关问题。此外，训练数据中本身就存在噪声和错误信息，模型在学习过程中不可避免地吸收了这些不准确的内容。

## 主流解决方案对比

目前解决幻觉问题的方案主要有三种。第一种是模型微调（Fine-tuning），即使用高质量的领域数据对模型进行二次训练，使其在特定领域更加准确。但微调的成本高昂，需要大量标注数据，而且模型知识仍然是静态的。第二种是事实核查（Fact-checking），通过外部工具或模型对生成内容进行验证，但这增加了系统的复杂度和延迟。第三种就是检索增强生成（RAG），在生成前引入外部知识作为上下文依据。

## 为什么 RAG 是最实用的方案

在三种方案中，RAG 被广泛认为是目前最实用、性价比最高的解决方案。它无需重新训练模型，部署和迭代成本低；知识库可以实时更新，确保信息时效性；回答的每一个论据都可以追溯到具体的源文档，便于用户验证。更重要的是，RAG 可以与微调和事实核查结合使用，形成多层防护。对于企业用户来说，RAG 的透明性和可控性使其成为构建可信 AI 应用的首选架构。`,
  },
];

export const PRESET_QUESTIONS: { question: string; description: string }[] = [
  { question: 'RAG 是什么？它是如何工作的？', description: '基础概念' },
  { question: '向量搜索和关键词搜索有什么区别？', description: '检索对比' },
  { question: '大语言模型为什么会产生幻觉？RAG 如何解决这个问题？', description: '幻觉问题' },
  { question: 'HNSW 索引是什么？为什么向量数据库需要它？', description: '索引原理' },
  { question: '如何评估 RAG 系统的回答质量？', description: '质量评估' },
];
